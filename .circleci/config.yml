version: 2
jobs:
  build:
    docker:
      - image: rocker/tidyverse:latest
    steps:
      - checkout
      - run: | 
        echo "hello world" 
            echo "$magicvar"
            echo "$KEY"
            echo "$CIRCLE_USERNAME"
            echo "${CIRCLE_PR_USERNAME:-not a fork}"
workflows:
  version: 2
  build_and_test:
    jobs:
      - build
